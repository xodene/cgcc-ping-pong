/*
 * Ball Class intended for an Atari ping pong clone.
 */
class Ball {
    /*
    Constructs a new ball given a position (x, y) and a ball radius
    */
    constructor(x, y, radius) {
        //TO-DO
    } 

    /*
    Draws our ball given a canvas 2D drawing context
    */
    draw(context) {
        //TO-DO
    }

    /*
    Constantly moves the ball by its current velocity
    */
    update() {
        //TO-DO
    }
}