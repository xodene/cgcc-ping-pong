/*
 * Paddle Class intended for an Atari ping pong clone.
 */
class Paddle {
    /*
    Constructs our Paddle given a position: x, y, and given dimensions: width, height
    */
    constructor(x, y, width, height) {
        //TO-DO
    }

    /*
    * Draws this paddle given a canvas 2D drawing context
    */
    draw(context) {
        //TO-DO
    }
}